class Car {
  int id;
  String model;
  String make;
  int year;
  double price;
  String description;
  String status;

  Car(this.id, this.model, this.make, this.year, this.price, this.description,
      this.status);
}
